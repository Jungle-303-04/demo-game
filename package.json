{
  "name": "demo-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Opsia와 연동하는 GPL-3.0 라이브 데모 게임",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:survev": "pnpm --dir upstream-survev --filter @survev/server build && pnpm --dir upstream-survev --filter @survev/client build",
    "typecheck": "tsc -p tsconfig.json --noEmit && pnpm --dir upstream-survev --filter @survev/server typecheck",
    "lint": "node scripts/lint.mjs",
    "test": "npm run build && npm run build:survev && node --test $(find dist/tests -name '*.test.js' -print)",
    "test:unit": "npm run build && node --test $(find dist/tests/unit -name '*.test.js' -print)",
    "test:e2e": "npm run build && node --test $(find dist/tests/e2e -name '*.test.js' -print)",
    "test:compose": "docker compose down -v && docker compose up --build -d && COMPOSE_E2E=1 npm run build && COMPOSE_E2E=1 node --test dist/tests/e2e/compose-api.test.js",
    "test:resilience": "bash scripts/compose-resilience-e2e.sh",
    "start:game": "cd upstream-survev/server && OPSIA_ROOM=true OPSIA_INFINITE=true SURVEV_CLIENT_DIR=$PWD/../client/dist node --enable-source-maps dist/gameServer.js",
    "start:api": "node dist/services/api-server/src/main.js",
    "start:orchestrator": "node dist/services/room-orchestrator/src/main.js",
    "start:ops": "node dist/services/ops-console/src/main.js",
    "botctl": "node dist/services/bot-runner/src/botctl.js",
    "demo:reset": "node dist/scripts/demo-reset.js"
  },
  "dependencies": {
    "prom-client": "^15.1.3",
    "redis": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=22"
  }
}
