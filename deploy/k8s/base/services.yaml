apiVersion: v1
kind: Service
metadata: { name: game-room-0, namespace: sandbox, labels: { app: game-server, opsia.dev/room-id: room-0, game.opsia.dev/room-id: room-0 } }
spec:
  selector: { app: game-server, opsia.dev/fleet: live, opsia.dev/room-id: room-0 }
  ports: [{ name: http, port: 8001, targetPort: http }]
---
apiVersion: v1
kind: Service
metadata: { name: game-room-1, namespace: sandbox, labels: { app: game-server, opsia.dev/room-id: room-1, game.opsia.dev/room-id: room-1 } }
spec:
  selector: { app: game-server, opsia.dev/fleet: live, opsia.dev/room-id: room-1 }
  ports: [{ name: http, port: 8001, targetPort: http }]
---
apiVersion: v1
kind: Service
metadata: { name: game-room-2, namespace: sandbox, labels: { app: game-server, opsia.dev/room-id: room-2, game.opsia.dev/room-id: room-2 } }
spec:
  selector: { app: game-server, opsia.dev/fleet: live, opsia.dev/room-id: room-2 }
  ports: [{ name: http, port: 8001, targetPort: http }]
---
apiVersion: v1
kind: Service
metadata: { name: game-room-3, namespace: sandbox, labels: { app: game-server, opsia.dev/room-id: room-3, game.opsia.dev/room-id: room-3 } }
spec:
  selector: { app: game-server, opsia.dev/fleet: live, opsia.dev/room-id: room-3 }
  ports: [{ name: http, port: 8001, targetPort: http }]
---
apiVersion: v1
kind: Service
metadata: { name: game-room-4, namespace: sandbox, labels: { app: game-server, opsia.dev/room-id: room-4, game.opsia.dev/room-id: room-4 } }
spec:
  selector: { app: game-server, opsia.dev/fleet: live, opsia.dev/room-id: room-4 }
  ports: [{ name: http, port: 8001, targetPort: http }]
---
apiVersion: v1
kind: Service
metadata:
  name: game-metrics
  namespace: sandbox
  labels: { app: game-server, opsia.dev/fleet: live }
spec:
  selector: { app: game-server, opsia.dev/fleet: live }
  ports: [{ name: http, port: 8001, targetPort: http }]
